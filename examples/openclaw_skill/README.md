# OpenClaw skill: budgeted task spend

A toy OpenClaw-style skill that spends from a per-task budget by calling Stipend
for each charge. Same wiring as the Hermes example; different framing
(charges instead of payments).

## Run

```
pip install stipend
cd examples/openclaw_skill
python stipend_skill.py
```
